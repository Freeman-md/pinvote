<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <meta http-equiv="Content-Type" content="text/html;charset=UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lato:wght@100;300;400;700;900&display=swap" rel="stylesheet">

  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-7767ZFVKQF"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag() { dataLayer.push(arguments); }
    gtag('js', new Date());

    gtag('config', 'G-7767ZFVKQF');
  </script>

  <script src="https://cdn.tailwindcss.com"></script>
  <script src="/js/tailwind.js"></script>

  <style type="text/tailwindcss">
    @layer utilities {
      * {
        box-sizing: border-box;
      }

        body {
          padding: 0;
          margin: 0;
          font-family: 'Lato', sans-serif;
        }
        
        .form-control {
          @apply w-full
        }

        .form-control label {
          @apply block text-sm
        }

        .form-control input {
          @apply px-4 py-2 mt-1 inline-block border transition duration-200 rounded-full focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary w-full
        }

        .form-control select {
          @apply mt-1 block w-full bg-white border border-gray-300 rounded-full py-2 px-4 leading-tight focus:outline-none focus:border-primary focus:ring-2 focus:ring-offset-2 focus:ring-primary
        }

        .form-control button {
          @apply inline-block
        }

        .form-group {
          @apply flex space-x-4 items-center
        }

        .btn {
          @apply px-4 py-2 text-center rounded-full transition duration-200 border hover:bg-gray-200 focus:ring focus:ring-offset-2 focus:ring-gray-500
        }

        .btn-primary {
          @apply bg-primary hover:bg-primary/90 text-white !border-none focus:!ring-primary
        }

        .btn-secondary {
          @apply bg-secondary hover:bg-secondary/90 text-white !border-none focus:!ring-secondary
        }

        .badge {
          @apply inline-block py-1 px-3 rounded-full min-w-fit
        }

        .badge-primary {
          @apply text-primary bg-primary/10
        }

        .badge-success {
          @apply text-green-500 bg-green-500/10
        }

        .badge-danger {
          @apply text-red-500 bg-red-500/10
        }
      }
    </style>

  <script src="/js/post-data.js"></script>
  <script src="/js/utilities.js"></script>
</head>